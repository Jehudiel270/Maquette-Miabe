import { CreateTontineScreen } from "@/components/create-tontine/CreateTontineScreen";

export const metadata = {
  title: "Créer une tontine — TontineChain",
  description: "Créez votre tontine blockchain sécurisée",
};

export default function CreateTontinePage() {
  return <CreateTontineScreen />;
}
